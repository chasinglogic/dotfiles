#!/bin/bash

cd /home
echo "Starting backup..."
rsync --delete --exclude $USER/.local/share/containers --exclude $USER/Dropbox --exclude $USER/.cache -avz $USER $@
echo "Backup complete with code: $?"
exit $?
